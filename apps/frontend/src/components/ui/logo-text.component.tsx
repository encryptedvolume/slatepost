import { Logo } from '@gitroom/frontend/components/new-layout/logo';

/**
 * One lockup ships — components/new-layout/logo.tsx. This file is kept only as
 * the import path the auth and billing screens already use.
 */
export const LogoTextComponent = Logo;
