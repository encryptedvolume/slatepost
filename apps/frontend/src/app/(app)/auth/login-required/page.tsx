export default async function LoginRequiredPage() {
  return (
    <div className="fixed left-0 top-0 w-full h-full bg-canvas z-[100] flex justify-center items-center t-title-1">
      Login to use the wizard to generate API code
    </div>
  );
}
